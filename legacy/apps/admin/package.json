{
  "name": "admin",
  "version": "0.1.0",
  "description": "UI playground using @repo/ui. The main product is apps/dm (http://localhost:3152). Use port 3153 here so both can run locally.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "next dev --port 3153",
    "build": "next build",
    "start": "next start --port 3153",
    "lint": "eslint --max-warnings 0",
    "check-types": "next typegen && tsc --noEmit"
  },
  "dependencies": {
    "@repo/ui": "*",
    "next": "16.1.5",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/node": "^22.15.3",
    "@types/react": "19.2.2",
    "@types/react-dom": "19.2.2",
    "eslint": "^9.39.1",
    "typescript": "5.9.2"
  }
}
